import {render,screen} from '@testing-library/react';import {MemoryRouter} from 'react-router-dom';import {describe,it,expect,vi} from 'vitest';import {App} from './App';
vi.mock('./lib/auth',()=>({authReady:false,observeUser:()=>()=>{},loginGoogle:vi.fn(),logout:vi.fn()}));
describe('Dilmaç',()=>{it('ana sayfayı ve çağrıyı gösterir',()=>{render(<MemoryRouter future={{v7_startTransition:true,v7_relativeSplatPath:true}}><App/></MemoryRouter>);expect(screen.getByRole('heading',{level:1,name:/Konuş/i})).toBeInTheDocument();expect(screen.getByRole('heading',{name:'İki ses, tek konuşma.'})).toBeInTheDocument();expect(screen.getAllByText('Canlı çeviriyi başlat').length).toBeGreaterThan(0)});it('oda hazırlık sayfasını açar',()=>{render(<MemoryRouter future={{v7_startTransition:true,v7_relativeSplatPath:true}} initialEntries={['/uygulama']}><App/></MemoryRouter>);expect(screen.getByRole('heading',{name:'Konuşma odanızı açın.'})).toBeInTheDocument();expect(screen.getByLabelText('Oda kodu')).toHaveAttribute('maxlength','6')})});
