export type TranslationResult={text:string;demo:boolean};
const demo:Record<string,string>={'merhaba':'Hello','nasılsın':'How are you?','bugün hava çok güzel':'The weather is beautiful today.','teşekkür ederim':'Thank you.','toplantı saat kaçta':'What time is the meeting?'};
export async function translate(text:string,target:string,_legacyKey?:string):Promise<TranslationResult>{const apiBase=import.meta.env.VITE_DILMAC_API_URL?.replace(/\/$/,"");if(!apiBase){const match=Object.entries(demo).find(([k])=>text.toLocaleLowerCase('tr').includes(k));return{text:match?.[1]||`[${target}] ${text}`,demo:true}}const res=await fetch(`${apiBase}/translate`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text,target})});if(!res.ok)throw new Error('Çeviri servisine ulaşılamadı.');const json=await res.json();return{text:json.text||'',demo:false}}
